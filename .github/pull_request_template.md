## Summary

Describe what changed and why.

## Verification

- [ ] GTK application builds with warnings treated as errors
- [ ] Core-only configuration builds without GTK
- [ ] Relevant tests pass locally
- [ ] Windows UCRT64 and Debian 13 CI jobs pass
- [ ] User-facing documentation is updated when needed

## Architecture

Note any new module boundaries, ownership rules, or deliberate departures from
`docs/ARCHITECTURE.md`. Write `None` when the architecture is unchanged.
